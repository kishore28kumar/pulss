# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=pulssdb

# MongoDB Configuration (for branding features)
MONGODB_URI=mongodb://localhost:27017/pulss_branding

# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Super Admin Configuration
SUPER_ADMIN_EMAIL=superadmin@pulss.app

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# CORS Configuration
# For multiple origins, separate with commas: http://localhost:5173,https://yourdomain.com
CORS_ORIGIN=http://localhost:5173

copilot/enhance-security-privacy-compliance
# Security Configuration
# Rate Limiting (requests per window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5
ACCOUNT_CREATION_LIMIT_MAX=3

# HTTPS Enforcement (set to true in production)
ENFORCE_HTTPS=false
# Push Notifications Configuration
FCM_ENABLED=false
FCM_SERVER_KEY=your_fcm_server_key

# ============================================================================
# ADVANCED NOTIFICATIONS SYSTEM
# ============================================================================

# Email Notifications
EMAIL_NOTIFICATIONS_ENABLED=true
EMAIL_PROVIDER=smtp
# Options: sendgrid, ses, smtp, msg91

# SendGrid Configuration (if using SendGrid)
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# AWS SES Configuration (if using SES)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# SMTP Configuration (if using SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password

# MSG91 Configuration (Indian provider)
MSG91_AUTH_KEY=your_msg91_auth_key
MSG91_SENDER_ID=your_sender_id

# SMS Notifications
SMS_NOTIFICATIONS_ENABLED=true
SMS_PROVIDER=twilio
# Options: twilio, gupshup, textlocal, msg91

# Twilio Configuration (if using Twilio)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Gupshup Configuration (Indian provider)
GUPSHUP_API_KEY=your_gupshup_api_key
GUPSHUP_USER_ID=your_gupshup_user_id

# Textlocal Configuration (Indian provider)
TEXTLOCAL_API_KEY=your_textlocal_api_key
TEXTLOCAL_SENDER=TXTLCL

# Push Notifications
PUSH_NOTIFICATIONS_ENABLED=true
PUSH_PROVIDER=fcm
# Options: fcm, apns

# Firebase Cloud Messaging (FCM)
FCM_SERVER_KEY=your_fcm_server_key

# Apple Push Notification Service (APNs)
APNS_KEY_ID=your_apns_key_id
APNS_TEAM_ID=your_apns_team_id
APNS_BUNDLE_ID=com.yourapp.bundle

# Webhook Notifications
WEBHOOK_NOTIFICATIONS_ENABLED=true
WEBHOOK_RETRY_ATTEMPTS=3
WEBHOOK_TIMEOUT=10000
WEB_PUSH_ENABLED=false
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# SMS/WhatsApp Configuration (Twilio)
TWILIO_ENABLED=false
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_WHATSAPP_NUMBER=+1234567890

# WhatsApp Business API Configuration
WHATSAPP_BUSINESS_ENABLED=false
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_API_KEY=your_whatsapp_api_key
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
main

# Optional: External Services
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=pulss-media

copilot/integrate-n8n-with-pulss
# n8n Workflow Automation Integration
N8N_ENABLED=false
N8N_WEBHOOK_URL=http://localhost:5678
N8N_API_KEY=
N8N_TIMEOUT=5000

copilot/add-advanced-notification-system
# Email/SMTP Configuration (for notification emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_email_password
SMTP_FROM_EMAIL=noreply@pulss.app
SMTP_FROM_NAME=Pulss Platform

# Email Service Configuration
# Choose email provider: 'nodemailer' (SMTP) or 'sendgrid' (API)
EMAIL_PROVIDER=nodemailer
EMAIL_FROM=noreply@pulss.app

# Nodemailer (SMTP) Configuration - for local development and custom SMTP servers
# Examples for common providers:
# Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_SECURE=false
# Outlook: SMTP_HOST=smtp-mail.outlook.com, SMTP_PORT=587, SMTP_SECURE=false
# Custom: Set your own SMTP server details
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# SendGrid Configuration - for production email delivery
# Get your API key from https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
feature/auth-system

# Security Scanning
# SNYK_TOKEN=your_snyk_token

# ============================================================================
# BILLING & PAYMENT GATEWAY CONFIGURATION
# ============================================================================

# Razorpay Configuration
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# Cashfree Configuration
CASHFREE_APP_ID=your_cashfree_app_id
CASHFREE_SECRET_KEY=your_cashfree_secret_key
CASHFREE_MODE=TEST

# Paytm Configuration
PAYTM_MID=your_paytm_merchant_id
PAYTM_MERCHANT_KEY=your_paytm_merchant_key
PAYTM_MODE=STAGING

# Email Configuration (for billing notifications)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=your_sendgrid_api_key
EMAIL_FROM=billing@pulss.app
EMAIL_FROM_NAME=Pulss Billing

# GST Configuration
GST_RATE=18
PLATFORM_GSTIN=YOUR_PLATFORM_GSTIN
PLATFORM_STATE=Your State
PLATFORM_STATE_CODE=00

# Billing Features
USAGE_BILLING_ENABLED=true
COUPON_SYSTEM_ENABLED=true
TRIAL_MANAGEMENT_ENABLED=true
E_INVOICING_ENABLED=true
AUTO_BILLING_ENABLED=true

main
